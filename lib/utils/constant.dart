// Enum
import 'dart:ui';

enum OptionType {
  useDefault,
  useConfigure,
}

enum FieldRequire {
  single,
  dual,
}

enum ConnectionStatus {
  great,
  good,
  poor,
  disconnected,
}

enum ZoneStatus {
  outside,
  inside,
}

const String fontName = 'Poppins';

const Color yellowBright = Color(0xFFffd380);
const Color yellowMid = Color(0xFFcaa75f);
const Color yellowDark = Color(0xFF987d49);
const Color white = Color(0xFFFFFFFF);
const Color dark = Color(0xFF000000);

const String kUser = "kUSERVALIDITY";
const String kUsername = "kUSERNAME";

const String url = "http://gamuda.primaclouds.com/api/location";
